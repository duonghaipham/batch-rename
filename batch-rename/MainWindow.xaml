<Window x:Class="batch_rename.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:batch_rename"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button>Refresh</Button>
                <Button>Help</Button>
                <Button>Start Batch</Button>
            </ToolBar>
        </ToolBarTray>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <ToolBarTray DockPanel.Dock="Top">
                    <ToolBar>
                        <TextBlock
                            Padding="2"
                            VerticalAlignment="Center">
                            Method:
                        </TextBlock>
                        <ComboBox
                            x:Name="cbMethodChooser">
                            <ComboBoxItem>New Case</ComboBoxItem>
                            <ComboBoxItem>Move</ComboBoxItem>
                        </ComboBox>
                        <Button Click="btnAddMethod_Click">
                            Add
                        </Button>
                        <Button Click="btnClearMethod_Click">
                            Clear
                        </Button>
                    </ToolBar>
                    <ToolBar>
                        <TextBlock
                            Padding="2"
                            VerticalAlignment="Center">
                            Preset:
                        </TextBlock>
                        <Button>Open</Button>
                        <Button>Save</Button>
                    </ToolBar>
                </ToolBarTray>
                <ListView>
                </ListView>
            </DockPanel>
            <TabControl Grid.Column="1">
                <TabItem Header="Rename Files">
                    
                </TabItem>
                <TabItem Header="Rename Folders">
                    
                </TabItem>
            </TabControl>
        </Grid>
    </DockPanel>
</Window>
